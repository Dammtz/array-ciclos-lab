// Sugerencia join con el siguiente codigo. 

let arr = ["This", "is", "a", "sentence."];
function printOutString () {
    return arr.join(" ");
}

// console.log (printOutString());

export { printOutString };